---
description: Sphinx documentation best practices
globs: docs/**/*.rst
alwaysApply: false
---
- Use `sphinx-quickstart`, enable `autodoc` & `napoleon`. :contentReference[oaicite:5]{index=5}
- Follow Google or NumPy style docstrings.
- Use `sphinx-apidoc` and include modules in `toctree`.
- Exclude build artifacts (`docs/_build/`) from version control.